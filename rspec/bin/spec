#!/usr/bin/env ruby

# rspec_dir = File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
# $LOAD_PATH.unshift(rspec_dir) unless $LOAD_PATH.include?(rspec_dir)
# require 'spec/autorun'



rspec_dir = File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
$LOAD_PATH.unshift(rspec_dir) unless $LOAD_PATH.include?(rspec_dir)




puts "ok, load path is:"
puts $:.inspect
a = File.join(Dir.getwd, 'opals/opalruby/spec/**/*.rb')
puts a
puts Dir.glob(a)

# require 'opalspec'

# Dir.glob(File.join(Dir.getwd, 'opals/opalruby/spec/**/*.rb')).each do |rb|
  # puts "globbing over: #{rb}"
  # require rb
# end

# Spec::Runner.run
