mode :all do
  config :browser,
    :required => [:opal],
    :bin_file => :browser,
    :include_bin => true
end

mode :spec do
  config :browser,
    
    :lib_directories    => ['lib', 'spec'],
    :bin_file           => :spec
end
