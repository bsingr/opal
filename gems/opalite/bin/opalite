#!/usr/bin/env ruby -wKU

opalite_dir = File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
$LOAD_PATH.unshift(opalite_dir) unless $LOAD_PATH.include?(opalite_dir)
require 'opalite'

Opalite::Runner.run ARGV
