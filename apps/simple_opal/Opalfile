
# for all modes, set the bin to foundation (will be cherry_kit)
mode :all do
  
  config :foundation,
    # tell foundation we want to use it's bin files
    :include_bin => :true
    
  config :simple_opal,
    :required => [:foundation]
end

# for soec mode, set the bin to spec, and also require the spec opal itself
mode :spec do
  
  config :spec, 
    :include_bin => :true
  
  config :simple_opal, 
    :required => [:foundation, :spec],
    :bin_file => :spec
end
