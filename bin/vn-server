#!/usr/bin/env ruby

require File.expand_path(
  File.join(File.dirname(__FILE__), %w[.. lib vienna]))
  
  
# Vienna::Tools.new :server

puts "running server"

project = Vienna::Project.new Dir.getwd, {}

puts "project: #{project}"

server = Vienna::AppServer.new project
