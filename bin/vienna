#!/usr/bin/env ruby

require File.expand_path(
  File.join(File.dirname(__FILE__), %w[.. lib vienna]))

case ARGV[0]
when 'build'
  Vienna::Tools.build
when 'clean'
  Vienna::Tools.clean
when 'cappbuild'
  Vienna::Tools.cappbuild
else
  puts 'Use either "build" or "cappbuild"'
end
